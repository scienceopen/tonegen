language: c

notifications:
  email: false

git:
  depth: 3

os: 
    - linux
    - osx


script: 
    - cc -o tonegen tonegen.c -lm
    - ./tonegen -d - -t 0.1 -r 48000 -c 1 > test.raw
    - test $(du test.raw -b | cut -f 1) -lt 20000 
# -eq 17644
